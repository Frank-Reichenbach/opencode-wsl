# Normalize all text files to LF; override per extension below.
* text=auto eol=lf

# Force LF for shell scripts so ShellCheck and WSL work correctly on Windows.
*.sh text eol=lf

# Force CRLF for PowerShell scripts to match Windows conventions.
*.ps1 text eol=crlf
